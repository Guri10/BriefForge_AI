# Copy to .env and fill when you add integrations (not used by scaffold).

# --- Future: OpenAI (agent-service) ---
# OPENAI_API_KEY=

# --- Future: NewsAPI (mcp-news-server only) ---
# NEWSAPI_KEY=

# --- HTTP boundaries (local dev defaults) ---
MCP_NEWS_SERVER_URL=http://localhost:8001
AGENT_SERVICE_URL=http://localhost:8000

# apps/web (Vite): prefix required for client exposure
VITE_AGENT_SERVICE_URL=http://localhost:8000
